spring:
  datasource:
    url: jdbc:mysql://localhost:3306/planwith
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   # 엔티티 변경 시 테이블 자동 업데이트
    show-sql: true